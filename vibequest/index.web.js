import { AppRegistry } from 'react-native';
import App from './App';

AppRegistry.registerComponent('vibequest', () => App);

AppRegistry.runApplication('vibequest', {
  rootTag: document.getElementById('root'),
});
